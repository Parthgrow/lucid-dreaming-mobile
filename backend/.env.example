# Server
PORT=3000

# Comma-separated list of allowed origins for browser clients (e.g. the web app).
# Leave unset in dev to allow all origins.
CORS_ORIGIN=http://localhost:5173

# Auth
JWT_SECRET=

# Firebase Admin SDK
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
